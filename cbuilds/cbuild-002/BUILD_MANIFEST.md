# cbuild-002 — Week 1

## Scope
Master architecture, repository/domain contracts, configuration, normalized event model, security boundaries and deterministic core tests.

## Safety gate
Live trading remains disabled by default. No private keys, RPC secrets or signing code are included.

## Test gate
Core configuration, risk-model validation and import structure must pass before the next cbuild.
