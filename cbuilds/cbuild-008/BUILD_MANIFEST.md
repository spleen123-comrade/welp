# Welp cbuild-008 — Week 7

EVM security foundation: control-state model, fail-closed analyzer, common selector hints, and deterministic security tests.

Important: selector matching is evidence only. It never independently triggers a buy or exit. Production security will add ABI decoding, simulation, controller discovery, proxy analysis, and chain-specific execution/state verification.